{
  "name": "word-unscrambler",
  "version": "1.0.0",
  "description": "This repository is for an API that allows the user to search words against a dictionary, return all possibilities and substring of a character set, and then run them against the dictionary.",
  "main": "index.js",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "author": "josiah sprankle",
  "license": "ISC"
}
